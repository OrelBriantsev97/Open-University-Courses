 
#define N 10
#include <stdio.h>
#include <stdbool.h>
#include <math.h>

void getMatrix(int[N][N]);
void isMagicSquare(int[N][N],int);
void printMatrix(int[N][N],bool);
bool isValid(int[N][N],int);
bool checkSum(int [N][N]);
bool sumRowCol (int[N][N],int);
bool sumDiagonal(int[N][N],int);
bool stopProg(int[N][N],int);

 

